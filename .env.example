# ==============================================================================
# TradeMitra Environment Configuration
# Author: Sumer Kumar
# ==============================================================================

# Server Configuration
NODE_ENV=development
PORT=4000
API_PREFIX=/api/v1
CORS_ORIGIN=http://localhost:5173

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trademitra
DB_USER=trademitra
DB_PASSWORD=@S@8949470181
DB_SSL=false

# Connection Pool Settings (Strict Enterprise Specs)
DB_POOL_MAX=20
DB_POOL_IDLE_TIMEOUT_MS=30000
DB_POOL_CONN_TIMEOUT_MS=5000
DB_POOL_MAX_USES=7500

# Rate Limiting Settings
RATE_LIMIT_GLOBAL_WINDOW_MS=900000
RATE_LIMIT_GLOBAL_MAX=100
RATE_LIMIT_ORDER_WINDOW_MS=5000
RATE_LIMIT_ORDER_MAX=3

# MCP / Real-time Stock Feed (india-stock-mcp)
MCP_FEED_ENDPOINT=http://localhost:8000/feed
MCP_FEED_RECONNECT_INTERVAL_MS=3000

# ==============================================================================
# SMTP Email Configuration (For Sending Real Email OTPs)
# ==============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=prevopapertrading@gmail.com
SMTP_PASS=trip skkg uxtm hoza
EMAIL_FROM="PREVO Trading" <prevopapertrading@gmail.com>

